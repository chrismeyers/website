{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=web",
    "dev:api": "npm run dev --workspace=api"
  },
  "engines": {
    "node": ">= 18",
    "npm": ">= 8"
  }
}
